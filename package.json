{
  "name": "figma-widget-contrast-card",
  "version": "1.0.0",
  "description": "Figma widget to show color contrast values.",
  "scripts": {
    "build": "esbuild widget-src/code.tsx --bundle --outfile=dist/code.js --target=es6",
    "tsc": "tsc --noEmit -p widget-src",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "@figma/widget-typings": "*",
    "@types/apca-w3": "^0.1.3",
    "apca-w3": "^0.1.9",
    "esbuild": "*",
    "typescript": "*"
  },
  "dependencies": {
    "@types/wcag-contrast": "^3.0.3",
    "color-blend": "^4.0.0",
    "js-base64": "^3.7.5",
    "wcag-contrast": "^3.0.0"
  }
}